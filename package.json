{
  "name": "lis-platform",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter=./apps/* dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "db:reset": "bash scripts/db-reset.sh",
    "run-golden-datasets": "bash scripts/run-golden-datasets.sh"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "5.9.3"
  }
}
