---
description: Continuous-improvement capture for agentic dev friction. Use
  whenever something during a session was confusing, slow, wrong, or broke a
  skill/workflow step — or, just as importantly, worked unusually well and is
  worth preserving. Turns ONE real friction point into ONE small, targeted
  fix to a Skill, playbook, or GitHub-workflow doc, with a before/after shown
  and confirmed before anything is edited. Always logs the run to
  CHANGELOG.md, whether or not a file actually changed. Trigger this
  proactively — don't wait for the human to type the exact word "retro" —
  whenever they say something like "that was confusing," "log this," "we
  keep hitting this," or "that worked well, let's keep doing it."
argument-hint: (no arguments needed — or a short description of the friction)
allowed-tools: Bash, Read, Write, Edit, Grep, Glob, AskUserQuestion
---

Read and follow the retro checklist in full, exactly as written — it is the
actual source of the process, areas, and rules; this file is only the
entrypoint that makes it callable as a Skill by name:
~/work/lis-engineering/skills/workflow/retro/SKILL.md

Do not duplicate that file's content here. If it drifts from this pointer,
fix the path here or the content there — never fork a second copy of the
checklist itself.
