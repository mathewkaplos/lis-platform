# Status — 2026-07-30 (session 2)

## What's actually done (per real evidence)

- **FEAT-007 (Clinical validation harness) is PARTIALLY DONE this session — TASK-026 closed, TASK-027 mechanical deliverable landed, FEAT-007/TASK-027 both deliberately left OPEN.** Implementation Proposal `docs/plans/feat-007-clinical-validation-harness.md` (APPROVED). Confirmed directly from `gh issue view 16` before drafting: FEAT-007's Dependencies field lists only `FEAT-005`, not FEAT-006 — a parallel-eligible sibling, not a successor, contrary to the prior session's assumption that closing FEAT-006 was what unblocked it.
  - **TASK-026 (#85, CLOSED, PR #172)** — `packages/db/src/golden-dataset-check.ts`: proves `db/golden/chemistry-ranges-criticals.json` matches the live `reference_range` table exactly, in both directions. Same `tsx`-script/`lis_app`/tenant-context shape as `rls-isolation-check.ts` (TASK-024) — no test framework exists in this repo, so this doesn't introduce one for a single Postgres check. **This PR also built CI's first-ever migrate+seed step** — `pr.yml` previously provisioned a Postgres service but never used it. Found and fixed a real latent bug while wiring it: the CI service was named `lis_test`, but `0002_app_role.sql` hardcodes `GRANT CONNECT ON DATABASE "lis"` — would have failed the instant any migration actually ran in CI. Fixed by renaming the CI service to `lis` (migrations are never edited after the fact). Verified for real, both directions of drift: a deliberately drifted value and a deliberately deleted row were each caught and named exactly, locally and on the actual GitHub Actions run.
  - **TASK-027 (#86, OPEN — mechanical deliverable only, same PR #172)** — `db/golden/chemistry-ranges-criticals.json` (23 entries, 14 chemistry analytes) mirrors `db/seed/chemistry-catalog.sql`'s rows 1:1. **The feature's literal AC — "reviewed and signed off by the design-partner lab" — is NOT met and cannot be met by code alone**: every seeded value is generic literature data, explicitly marked "not partner-validated" in the seed's own header (a pre-existing gap, not introduced this session). Per explicit human decision this session ("Land TASK-026 now, track sign-off as an open follow-up"): landed the mechanical proof, left the AC checkbox unchecked, opened **issue #171** to track the real, external, human-process gap. #86 and #16 (FEAT-007) both stay OPEN on purpose — commented on both explaining why, not silently closed on the strength of the mechanical proof alone.
  - Authored the `engineering/testing` Skill (lis-engineering) — required by FEAT-007's own issue and didn't exist yet, same gap class FEAT-006 hit for `rls-multi-tenancy`. Documents the `tsx`-script-over-Vitest convention, the `lis_app`/tenant-context pattern, and the CI migrate→seed→run wiring pattern this task established.
- **M0 — Foundation & Walking Skeleton is CLOSED** (18/18 issues). Unchanged since last refresh.
- **FEAT-004 (Catalog metadata model) is CLOSED.** Unchanged since last refresh — see prior breadcrumb history for TASK-016–019 detail.
- **FEAT-005 (Observation store) is CLOSED.** Unchanged since last refresh — see prior breadcrumb history for TASK-020–022 detail.
- **FEAT-006 (Order, specimen & tenancy spine) is CLOSED** (prior session) — issue #15 closed, all three tasks merged. Implementation Proposal `docs/plans/feat-006-order-specimen-tenancy-spine.md` (APPROVED), all four §10 open questions resolved before implementation started (UUIDv4+sequence for `audit_event.id` rather than `pg_uuidv7`; CHECK-constrained text for `specimen.rejection_reason`; narrow `order`/`ordered_test` scope deferring priority/diagnosis/billing/provider; TASK-024 reinterpreted as a test-suite-only deliverable, no new RLS SQL).
  - **TASK-023 (#82, PR #165)** — `order`, `ordered_test`, `specimen`, `specimen_fulfillment` (M:N fulfilment, KB-22). `tenant_id` + RLS on all four tables in this same migration, per Constitution Law #4. `order.patient_id` follows ADR-0005's forward-reference pattern directly (no new ADR needed). Verified for real as `lis_app`: one specimen fulfils two distinct ordered tests (AC), aliquot lineage queryable, both `specimen` CHECK constraints (status, rejection_reason) reject invalid values, RLS isolates.
    - **Found and fixed a real drizzle-kit bookkeeping gap**: `0007`/`0008` (hand-written, TASK-021/022) never got drizzle-kit snapshots, so `generate` diffed against stale `0006` state and produced a migration that would have duplicated `0007`/`0008`'s already-applied DDL against any real database. Caught by inspection before running it, not by a failed migration. Recorded as a new entry in lis-engineering's `database-design` Skill.
  - **TASK-024 (#83, PR #166)** — reinterpreted per Constitution Law #4: no new RLS-policy SQL (already required in TASK-023's own migration). Delivered `packages/db/src/rls-isolation-check.ts` (`pnpm --filter @lis/db rls-check`) instead — a dynamic, table-list-free proof (structural sweep + live cross-tenant leak check) across every tenant table the schema actually has, discovered via `pg_class`/`information_schema`, not hardcoded. **Negative case proven for real**: RLS deliberately disabled on a real table, both checks correctly failed (and caught an actual cross-tenant read); re-enabled, clean pass again. Deliberately not wired into CI — that's FEAT-007/TASK-026's explicit job. Also authored the `rls-multi-tenancy` Skill FEAT-006's own issue required and which didn't exist yet.
  - **TASK-025 (#84, PR #167)** — `audit_event` (KB-11) with per-tenant hash chaining (`packages/db/src/audit.ts`: `writeAuditEvent`/`verifyAuditChain`). Uses a canonical, recursively key-sorted JSON serializer for `before`/`after`/`context` since Postgres jsonb doesn't preserve key insertion order (a real correctness finding — a naive re-serialization on read-back would otherwise produce false-positive tamper detections). **Found and fixed two real, previously-latent privilege gaps**, both only surfaced by actually running the migration against `lis_app`: (1) the proposal's own flagged risk — `0002_app_role.sql`'s blanket default-privilege grant would have silently made this table mutable, fixed with an explicit `REVOKE UPDATE, DELETE`; (2) a new one — `bigserial`'s implicit sequence object needs its own `GRANT USAGE, SELECT`, since the schema's default-privilege grant only ever covers tables, never sequences, and no earlier table used a serial column. AC verified for real: a direct `UPDATE` as `postgres` (simulating a compromised admin, since `lis_app` is correctly blocked) is detected by `verifyAuditChain` reporting `valid=false` with the correct `brokenAtId`.
- **#132 (Constitution gate proof) is CLOSED.** Unchanged since last refresh.
- **EPIC-001 (#1) is currently OPEN.** Unchanged since last refresh — no outstanding action.

## Currently active milestone

**M1 — Domain Core & Database Spine** (3 open issues, 16 closed — confirmed via GitHub Milestones API 2026-07-30, post-TASK-026 closure; supersedes the prior count of "4 open, 15 closed").

Highest-priority open items in M1 (all 3):
- **#86 (TASK-027) / #16 (FEAT-007)** — deliberately still open. Mechanical golden-dataset deliverable is done (see above); the remaining work is not engineering work — it's getting a real design-partner lab to review and sign off `db/golden/chemistry-ranges-criticals.json`, tracked in **#171**. Nothing to do here until that external review happens.
- **#145** — Design an ADR-based RLS-exemption mechanism for the Constitution gate. Still not urgent — no *global* (exempt) tenant table has been introduced since the last refresh; `order`/`specimen`/`audit_event` are all genuinely tenant-scoped. Noted again in the new `rls-multi-tenancy` Skill's own "not yet covered" section.

**New, not milestone-scoped:** **#171** — design-partner sign-off follow-up for TASK-027's golden dataset. External/process dependency, not an engineering task; re-check whether a design-partner relationship exists before assuming this is still blocked.

**Unresolved finding, carried forward unchanged:** **#74 (TASK-015)'s out-of-band closure** (no linked PR/commit) remains unverified from a prior session — not re-investigated this session; still flagged, not silently absorbed.

## Notes / gotchas for the next session

- **"Code merged" and "acceptance criterion proven" are different facts.** Confirmed again this session, at the AC/issue level rather than just the schema level: TASK-026's issue is closed because its AC is genuinely met, but TASK-027/FEAT-007 are deliberately left open even though their code landed in the same PR, because their literal AC (design-partner sign-off) isn't met and can't be by code alone. Also confirmed in the prior session (the drizzle-kit snapshot gap, the two audit_event privilege gaps). Keep applying this skepticism — including to issue-closure state, not just to schema/grant state.
- **A feature's own "Dependencies" field is ground truth — verify it directly before assuming sequencing.** This session's FEAT-007 proposal was drafted only after `gh issue view 16` confirmed its Dependencies field lists `FEAT-005` alone, not FEAT-006 — the prior session's breadcrumb had implied FEAT-007 was "next" because FEAT-006 had just closed, which was true chronologically but not causally. Don't infer a dependency from session ordering; read the issue.
- **CI's Postgres service database name must match the hardcoded name in `0002_app_role.sql`'s `GRANT CONNECT ON DATABASE "lis"`.** Found this session: `pr.yml`'s service was `lis_test`, which would have failed the first time any migration actually ran in CI (nothing had, until TASK-026). Fixed by renaming the CI service, not the migration (migrations are never edited after the fact). Any future CI Postgres service must stay named `lis`.
- **`golden-dataset-check.ts` (`pnpm --filter @lis/db golden-check`, or `pnpm run-golden-datasets` at the root) is now wired into CI** (`pr.yml`, after migrate+seed, before `pnpm test`) — this is also CI's first-ever database bootstrap step. Extend this dataset/checker for future disciplines rather than building a parallel one; see the new `engineering/testing` Skill.
- New `engineering/testing` Skill (lis-engineering) — documents the `tsx`-script-over-Vitest convention (no test framework exists in this repo yet), the `lis_app`/tenant-context connection pattern shared with `rls-multi-tenancy`, and the CI migrate→seed→run wiring pattern. Required reading before adding any future CI-run check.
- **Design-partner sign-off (#171) is a real, open, non-engineering gap** — every reference range and critical threshold in this repo (`db/seed/chemistry-catalog.sql`, and now `db/golden/chemistry-ranges-criticals.json`) is generic literature data, not clinically validated by any actual lab. Don't let a green golden-dataset-check read as clinical correctness; it only proves internal consistency.
- **Hand-written migrations need a manually reconciled drizzle-kit snapshot, or the next `generate` silently corrupts.** `database-design` Skill entry (lis-engineering) — see TASK-023's finding, prior session. Check this specifically any time a hand-written migration is followed by a `drizzle-kit generate` run.
- **`ALTER DEFAULT PRIVILEGES` only covers what it says it covers — check the actual grant, don't assume it's comprehensive.** Recurring risk class (prior session: audit_event's UPDATE/DELETE and its implicit sequence object; also `postgres`/`BYPASSRLS`, the `meta/*.json` gate false positive, the `ENABLE ROW LEVEL SECURITY` gate false negative). Always verify the actual `information_schema.role_table_grants` (or equivalent) state after any new migration that could plausibly interact with `lis_app`'s existing blanket grants.
- **`rls-isolation-check.ts` (`pnpm --filter @lis/db rls-check`)** remains the standing, reusable isolation proof — extend rather than replace. Still not wired into CI (only `golden-dataset-check.ts` is, as of this session) — that remains a separate, not-yet-scheduled gap, not assumed closed by this session's CI work.
- `lis_app` (non-superuser, `NOBYPASSRLS`) remains the correct role to connect as for any RLS/tenant-scoped verification, locally and in CI now; `postgres` is migrations/seed-only.
- `docker` needs Docker Desktop running on Windows with WSL integration enabled in this WSL distro; available and used without issue this session. `docker compose down -v` between verification runs remains the established clean-teardown pattern.
- **SSH firewall IP drift flagged at the start of the prior session is now resolved** — current egress IP matches `infra/terraform.tfvars`'s `ssh_allowed_ip` as of this session's own check; no outstanding remediation needed unless it drifts again.
- All figures above are current as of 2026-07-30 (session 2), gathered directly from `gh api`/`gh issue view`/`gh pr view`/`gh run list` and real `psql`/`tsx`/CI verification this session — re-verify against GitHub/the live DB directly if much time has passed rather than trusting this file blindly.
