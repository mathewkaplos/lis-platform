{
  "name": "lis-platform",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "pnpm --parallel --filter=./apps/* dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "db:reset": "bash scripts/db-reset.sh"
  }
}
